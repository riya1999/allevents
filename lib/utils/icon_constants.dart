import 'package:flutter/material.dart';

const Icon categoryIcon = Icon(Icons.category_outlined);

const Icon listIcon = Icon(Icons.list_outlined);

const Icon gridIcon = Icon(Icons.grid_view_outlined);